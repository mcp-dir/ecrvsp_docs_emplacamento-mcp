# Ferramentas

ECRVSP Documentos: Primeiro Emplacamento expõe 1 ferramenta (todas somente leitura).

### 1. `ecrvsp_docs_emplacamento_consultar`
**Input**: `a3`, `a3_pin`, `login_cpf`, `login_senha`, `placa_escolhida` (opcional), `placa_final` (opcional), `file_pdf` (opcional), `chassi`, `categoria`, `tipo_restricao`, `cpf` (opcional), `cnpj` (opcional), `rg` (opcional), `rg_orgao_expedidor` (opcional), `rg_uf` (opcional), `nfe` (opcional), `nfe_data_emissao`, `nfe_numero` (opcional), `nfe_valor` (opcional), `nome`, `nome_anterior`, `telefone` (opcional), `endereco_cep`, `endereco_numero`, `endereco_complemento` (opcional), `veiculo_pessoa_deficiencia` (opcional), `veiculo_taxi`, `veiculo_isencao_fiscal` (opcional), `veiculo_venda_dispensada_renave` (opcional), `veiculo_pessoa_deficiencia_venda_direta` (opcional), `veiculo_empresa_salvados` (opcional), `tipo_placa_dianteira` (opcional), `tipo_placa_traseira` (opcional), `tipo_placa_traseira_segunda` (opcional)

ECRVSP Documentos: Primeiro Emplacamento, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar ECRVSP Documentos: Primeiro Emplacamento
```
